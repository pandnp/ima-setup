# README

# create local symmetric and asymmetric keys

# *** edit evm_create_symkey.sh ***
# call with either TPM to create a trusted-key.
#   or NOTPM to create a user-type key.
./evm_create_keys.sh

# On reboot, as root, label filesystem:
# cd into directory
./label_files.sh
