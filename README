# necessary packages

# redhat
sudo yum install automake autoconf libtool openssl-devel libattr-devel

# ubuntu
sudo apt-get install automake autoconf libtool libssl-dev attr libattr1-dev keyutils libkeyutils-dev pkg-config
